local ScrollingFrame = require(script.ScrollingFrame)
local VirtualizedContent = require(script.VirtualizedContent)

local Components = {}

Components.ScrollingFrame = ScrollingFrame
Components.VirtualizedContent = VirtualizedContent

return Components
